name: Learn basic of GitHub Action

on:
  push:
    branches:
      -  main

jobs:
  Hello-World:
    runs-on: ubuntu-latest

    steps:
      - name: print hellow
        run: echo "hello world !!"
    steps:
      - name: print chello
        run: echo "hello world !!"
